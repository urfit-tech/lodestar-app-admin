import styled from 'styled-components'

export const EmptyAdminBlock = styled.div`
  display: grid;
  place-items: center;
  min-height: 560px;
  font-size: 14px;
  font-weight: 500;
  letter-spacing: 0.4px;
  color: var(--gray-dark);
`
