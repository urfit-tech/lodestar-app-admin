import React, { forwardRef } from 'react'

// TODO: 
const MultipleMemberSelector: React.FC<{}> = ({ }) => {
    return (
        <div></div>
    )
}

export default forwardRef(MultipleMemberSelector)